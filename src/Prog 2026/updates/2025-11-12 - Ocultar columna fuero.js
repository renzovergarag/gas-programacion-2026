function ocultarColumnaFuero() {
    const hoja = "DOTACION"; // Reemplaza con el nombre de tu hoja
    const rango = "Q:Q"; // Reemplaza con la columna que deseas ocultar
    ocultarColumnaEnBdProg(hoja, rango);
}
