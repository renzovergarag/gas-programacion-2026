function limpiarCeldaEstablecimiento() {
    const hoja = "ANTECEDENTES"; // Reemplaza con el nombre de tu hoja
    const rangoString = "B3"; // Reemplaza con el rango que deseas limpiar
    limpiarRangoEnBdProg(hoja, rangoString);
}