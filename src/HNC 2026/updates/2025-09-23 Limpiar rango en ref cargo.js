function limpiarRangoEnFilaTons() {
    const hoja = "REF_CARGO"; // Reemplaza con el nombre de tu hoja
    const rangoString = "E16:AY16"; // Reemplaza con el rango que deseas limpiar
    limpiarRangoEnBdHnc(hoja, rangoString);
}
